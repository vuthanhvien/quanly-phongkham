import { NextRequest, NextResponse } from 'next/server'
import { requireAdmin } from '../../../../lib/auth'
import { databaseNameFromUrl, managementDb, normalizeDomain, provisionTenantDatabase, Tenant } from '../../../../lib/db'
const view = (tenant: Tenant) => ({ id: tenant.id, domain: tenant.domain, databaseName: databaseNameFromUrl(tenant.databaseUrl), isActive: tenant.isActive, createdAt: tenant.createdAt, updatedAt: tenant.updatedAt })
export async function PATCH(request: NextRequest, { params }: { params: Promise<{ id: string }> }) { if (!requireAdmin(request)) return NextResponse.json({ message: 'Không có quyền' }, { status: 401 }); try { const body = await request.json(); const repo = (await managementDb()).getRepository(Tenant); const current = await repo.findOneBy({ id: (await params).id }); if (!current) return NextResponse.json({ message: 'Không tìm thấy tenant' }, { status: 404 }); const databaseUrl = body.databaseName === undefined ? current.databaseUrl : (await provisionTenantDatabase(String(body.databaseName))).databaseUrl; return NextResponse.json({ data: view(await repo.save({ ...current, ...(body.domain !== undefined ? { domain: normalizeDomain(body.domain) } : {}), databaseUrl, ...(typeof body.isActive === 'boolean' ? { isActive: body.isActive } : {}) })) }) } catch (error) { return NextResponse.json({ message: error instanceof Error ? error.message : 'Không thể lưu tenant' }, { status: 400 }) } }
